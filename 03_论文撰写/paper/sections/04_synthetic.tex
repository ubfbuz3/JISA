\section{Synthetic study: where the adjudication goes}
\label{sec:synthetic}

\subsection{Definitions}
\label{sec:metrics}

Confusions are defined over the \emph{verdict}: a \emph{false proof} is the
strict subset of false alarms promoted to \texttt{violation-proven}---an error
the tool asserts as established. We also report the \emph{detection-keep rate}
(share of fired true violations still alarmed) and count only abstentions
justified by an unreadable oracle.

\subsection{What the constructed system shows}

\paragraph{Attribution is clean} On the anchored configuration the false-proof
rate is \ResFpEthreeHits{}/\ResFpEthreeTotal{} = \ResFpEthreePct{}\%
($[\ResFpEthreeLo{}\%, \ResFpEthreeHi{}\%]$); with the legitimate-sharing
exception absent the rate is \ResBaseFpRate{}
(\ResBaseFpHits{}/\ResBaseFpTotal{}), and with the channel listed it is
\ResListedCtlFpRate{} (\ResListedCtlFpHits{}/\ResListedCtlFpTotal{}). The
phenomenon is therefore attributable to a legitimate sharing exception the MR
cannot know about, not to execution itself. A $2{\times}2$ grid over channel
visibility and enforcement correctness tells the same story from the other
side: on unlisted cells the false-proof rate is
\ResBEThreevulnZerounlistedFpPct{}\% with no false negatives; on listed cells it
drops to \ResBEThreevulnZerolistedFpPct{}\% but false negatives appear
(\ResBEThreevulnOnelistedFn{} of \ResBEThreevulnOnelistedTp{}+\ResBEThreevulnOnelistedFn{}
on the violating cell). Avoiding false proofs and keeping detection are
controlled by one switch, and the switch cannot be thrown both ways at once.

\paragraph{Filtering exceptions does not help}
The exception ladder---raw, one exception level, two---leaves the anchored rate
at \ResLadderrawRate{}, \ResLaddereOneRate{}, \ResLaddereOneeTwoRate{}: it does
not move. The exception levels we can express are themselves functions of the
same reachability model, so they inherit its blind spot.

\paragraph{The framework cannot see the channel that matters}
When a legitimate share was created through the framework's facility, its log
holds \ResObsFwLogRecords{} records and v2 produces \ResObsFwVtwoFalseProof{}
false proofs (rate \ResObsFwVtwoFpRate{}); when the same share was created out
of band, the log holds \ResObsOobLogRecords{} records---no evidence the share
exists---and v2's false proofs rise to \ResObsOobVtwoFalseProof{} of
\ResObsOobFalseAlarm{} alarms (rate \ResObsOobVtwoFpRate{}), every one promoted
to \texttt{violation-proven}. The MR cannot distinguish ``this share does not
exist'' from ``this share exists but was not crawled''; its only representation
of the first is the second. On S7, where a genuine violation \emph{does} sit
behind the out-of-band share, the same blindness makes the alarm right for the
wrong reason (\ResObsOobBolaVtwoProvenBola{} of \ResObsOobBolaBolaTotal{}
reached)---and the coincidence of alarms with violations there is exactly what
an input-level metric cannot unpack. The same tension appears within a single
MR: for S3 (must not alarm) vs S5 (must alarm), no configuration in the family
is correct on both---the choice of switch is a choice of \emph{which} error to
make.

\paragraph{Stratification, not aggregation}
Aggregated, v3 looks best (recovery \ResVdVThreeRecoveryRate{} against
\texttt{degrade\_all}'s keep rate \ResVdDegradeallKeepRate{}); the reading is
wrong, because v3 changes regime with oracle readability. Stratified: on the
readable stratum (\ResIntroOnN{} records) it removes v2's false proofs
(\ResIntroOnVthreeFpN{} vs.~\ResIntroOnVtwoFpN{}) while still alarming on all
\ResIntroOnVthreeProvenBola{} fired true violations; on the absent stratum
(\ResIntroOffN{}) it alarms on none of the \ResIntroOffVtwoProvenBola{} true
violations v2 still alarms on, abstaining \ResIntroOffAbstain{} times. Pooling
converts honest abstention into apparent performance, so v3's numbers may appear
only stratified.

\subsection{The decisive ablation: does the metamorphic step carry work?}
\label{sec:synthetic-ablation}

Our earlier iteration proposed v3 as a mechanism, resting on the claim that the
metamorphic step was necessary. We tested that claim by removing the step and
substituting the most naive consumer of the same observable. The distinction
matters: \texttt{direct\_only}---oracle yes, access outcome
ignored---produces \ResAbDirectOnlyFP{} false proofs against v3's
\ResAbVThreeFP{} (precision \ResAbDirectOnlyPrec{}), and rejecting it proves
nothing because it discards half the evidence. The fair control is
\texttt{direct\_observe}: a succeeding ordinary request plus the same readable
surface, consuming exactly what the MR-based adjudicator consumes.

\begin{table*}[t]
\centering
\caption{End-to-end confusion matrix over all \ResSynRecords{} records
(\ResAbNBola{} true violations, \ResAbNNormal{} correct cases). Intervals are
Wilson 95\% and are descriptive (Section~\ref{sec:stats}).
\texttt{direct\_only} is a deliberately unfair control, shown to make that
point.}
\label{tab:ablation}
\begin{tabular}{lrrrrrrl}
\toprule
method & TP & FP & FN & TN & precision & recall & F1 \\
\midrule
v1 (raw)              & \ResAbVOneTP{} & \ResAbVOneFP{} & \ResAbVOneFN{} & \ResAbVOneTN{} & \ResAbVOnePrec{} & \ResAbVOneRec{} & \ResAbVOneFone{} \\
v2 (exception-aware)  & \ResAbVTwoTP{} & \ResAbVTwoFP{} & \ResAbVTwoFN{} & \ResAbVTwoTN{} & \ResAbVTwoPrec{} & \ResAbVTwoRec{} & \ResAbVTwoFone{} \\
v3 (state-calibrated) & \ResAbVThreeTP{} & \ResAbVThreeFP{} & \ResAbVThreeFN{} & \ResAbVThreeTN{} & \ResAbVThreePrec{} & \ResAbVThreeRec{} & \ResAbVThreeFone{} \\
\texttt{degrade\_all}& \ResAbDegradeAllTP{} & \ResAbDegradeAllFP{} & \ResAbDegradeAllFN{} & \ResAbDegradeAllTN{} & \ResAbDegradeAllPrec{} & \ResAbDegradeAllRec{} & \ResAbDegradeAllFone{} \\
\texttt{direct\_only} & \ResAbDirectOnlyTP{} & \ResAbDirectOnlyFP{} & \ResAbDirectOnlyFN{} & \ResAbDirectOnlyTN{} & \ResAbDirectOnlyPrec{} & \ResAbDirectOnlyRec{} & \ResAbDirectOnlyFone{} \\
\texttt{direct\_observe} & \ResAbDirectObserveTP{} & \ResAbDirectObserveFP{} & \ResAbDirectObserveFN{} & \ResAbDirectObserveTN{} & \ResAbDirectObservePrec{} & \ResAbDirectObserveRec{} & \ResAbDirectObserveFone{} \\
\bottomrule
\end{tabular}
\end{table*}

Table~\ref{tab:ablation} closes this line of work. \texttt{direct\_observe} has
\ResAbDirectObserveFP{} false proofs against v3's \ResAbVThreeFP{}---a tie at
zero, not a win---with the same point-estimate precision
(\ResAbDirectObservePrec{}), and recall \ResAbDirectObserveRec{} against
\ResAbVThreeRec{}. The recall difference of \ResAbRecallDiff{} is not
distinguishable from zero (95\% CI
$[\ResAbRecallDiffLo{}, \ResAbRecallDiffHi{}]$): a descriptive, point-estimate
comparison, not domination; no margin was pre-specified, so no formal
hypothesis-test claim attaches to it. We found no measurable advantage of the
metamorphic step over direct observation in these oracle-available scenarios,
and that absence is decisive: the step would have to be better at something the
plain request cannot do, yet on every one of the \ResAbDisagreementN{}
disagreeing records it is v3 that fails, its reachability precondition blocking
a detection the plain request makes. Selective presentation would have hidden
the cost: v3's precision of \ResAbVThreePrec{} was bought with
\ResAbVThreeFN{} false negatives (F1 \ResAbVThreeFone{}), an exchange of
\ResAbVOneFP{} false alarms for missed violations dressed as an improvement.

\subsection{Reproducing the reported specificity, and why it cannot see this}

The reported specificity has the form
\[ S_{\text{input}} = 1 - \frac{\text{false alarms}}{\text{inputs}}. \]
We cannot reproduce the engine's exact input count, so we approximate two
follow-up inputs per case---enough to exhibit the denominator's resolution, the
only point at issue. In that approximation the configuration in which a whole
class of legitimate access is misreported still yields a specificity of
\ResSpecRowOneSpec{} (\ResSpecRowOneSpecPct{}\%). A failure uniform across a
scenario class is diluted by every other input in the run, and the predecessor
design says the same of itself, attributing its residual false alarms to crawler
coverage of ``all URLs for all users''~\cite{Mai2019Metamorphic}.

\paragraph{A proposition on the metric's resolution}
Define $S_{\mathrm{input}} = 1 - \mathit{FP}_{\mathrm{inputs}} / N_{\mathrm{inputs}}$
and $S_{\mathrm{scenario}} = 1 - N_{\mathrm{failed}} / N_{\mathrm{scenarios}}$,
where $\mathit{FP}_{\mathrm{inputs}}$ counts false alarms over all generated
inputs and $N_{\mathrm{failed}}$ the scenarios in which the failure is uniform.

\noindent\textbf{Proposition.} Fix $f \ge 1$ failed scenarios, each producing a
fixed number $a \ge 1$ of false-alarm inputs, and $s \ge 1$ successful
scenarios each contributing $m$ additional, alarm-free inputs. Then
$S_{\mathrm{input}} = 1 - (f a) / (f a + s m)$, which tends to $1$ as
$m \to \infty$, while $S_{\mathrm{scenario}} = 1 - f / (f + s)$ is independent
of $m$ and strictly less than $1$.

\noindent\textbf{Proof.} $N_{\mathrm{inputs}} = f a + s m$ and
$\mathit{FP}_{\mathrm{inputs}} = f a$; substitution gives
$S_{\mathrm{input}} = 1 - (f a) / (f a + s m) \to 1$ as $m \to \infty$, whereas
the scenario-level figure depends only on the fixed counts $f$ and $s$.
\hfill $\square$

\smallskip
\noindent\textbf{Corollary.} No fixed input-level specificity threshold
$t < 1$ can guarantee detection of a scenario-uniform failure class: choosing
$m > (\mathit{fa}/s)\cdot t/(1-t)$ drives $S_{\mathrm{input}}$ above $t$ while
the $f$ failing scenarios remain failing. \hfill $\square$

\smallskip
The metric is therefore not merely imprecise: it is arbitrarily dilutable with
respect to a scenario-uniform failure---adding unaffected inputs drives it
toward $1$ without touching the failure---so it lacks stable scenario-level
resolution.

\paragraph{Second implementation}
An independently written implementation of the same predicates agreed on
\ResAltAgree{} of \ResAltCompared{} records (\ResAltAgreeRate{}; Wilson 95\% CI
$[\ResAltAgreeRateLo{}, \ResAltAgreeRateHi{}]$). Both were written from the same
reading of the semantics, so this bounds implementation accident only; it does
not bound a shared blind spot, and we do not present it as corroboration.
