# 第五轮 · 答辩审判

> **审判方**：同族模型（跨厂商通道不可得，已知共同盲点缺口）
> **日期**：2026-09-18
> **对象**：`07_作者答辩_v5.md`
> **参照**：A 路 `05_五层追问_v5.md`、B 路 `06_拒稿备忘录_v5.md`、方案 `研究方案_v5.md`、实验 `EXPERIMENT_RESULTS.md`、代码 `mstwi_mrs.py` / `engine.py`、原始数据 `raw_matrix.jsonl`、MST-wi 原文 `mstwi_paper_text.txt` 与目录 `catalog_text.txt`

---

## 独立性声明与核实范围

本轮我**独立跑了代码与数据**，未仅采信答辩陈述。核实手段：`C:/Users/Administrator/.workbuddy/binaries/python/envs/bolaexp/Scripts/python.exe` 读 `raw_matrix.jsonl`（324 条）与 MST-wi 目录/全文文本。

**已独立核实并确认属实的事实：**
1. 目录词频：`userCanRetrieveContent`=**54**、`cannotReachThroughGUI`=8、`isSupervisorOf`=7、`isAdmin`=2、`notTried`=53；`owner/ownership/delegat/canAccess/hasAccess/ACL/belongsTo/granted` **全 0**。
2. `MR CWE_298_599` 确在 catalog line 1185；line **1192–1193** 的 `userCanRetrieveContent(action.user, Output(Input(1),pos))` 位于 `IMPLIES` 前置条件内（与 `isLogin` / `notTried` / `CREATE` 以 `&&` 连接）——**确作前置条件**，非仅 OR 分支项。
3. `V-E3-vuln0-unlisted @ gui_mode=aware @ S3`：6/6 全触发、conf 全为 **FP**（答辩"2/2 FP"属实，且**gui 谓词处于生效的 aware 状态**）。
4. `V-E3-vuln1-listed @ gui_mode=aware @ S5`：3 行，truth 全为 `bola`、trig 全 `False`、conf 全 **FN**（答辩"FN"属实）。
5. MST-wi 原文三句均命中 `mstwi_paper_text.txt`：**"provided to her by the GUI""、"not the functionality exposed by the user interface"、"mapping between roles and resources, which is not always available"** —— D5 引用**真实，非编造**。
6. 额外核实：遍历全部 (config, gui_mode) 组合，**不存在**任一配置使 S3（合法共享→应不触发）与 S5（只读共享下写越权→应触发）同时判对。

**未能独立核实（须作者补证，不默认成立）：**
- D6 称"授权类 MR 共 4 条（OTG_AUTHZ_001..004 / CWE-639 族）"——我仅移植核实了 MR-002 / MR-004 两条的 catalog 行号，未对另两条做逐行比对；此条标记为**需核实**，不计入已证实事实。
- R3 所依赖的"Nextcloud / Gitea / GitLab 对象级共享 + MST-wi GUI 爬取可覆盖"未在本环境验证，属作者主张。

---

## 逐条审判

### D1 · `userCanRetrieveContent` 与"对象级谓词零命中"

[DA-DECISION: Score 4/5 | Concede | REASON]

答辩**自认广义断言"对象级谓词零命中"为假**——这一点我独立核实：54 次命中 + 作前置条件，确凿。其窄化（"授权状态派生的对象级谓词为零，GUI 提供性谓词为 54/8"）与 MST-wi 原文定义一致，且可复核。A 路 / B 路据此推翻的广义 C1 已死亡，我不继续主张"MST-wi 完全没有对象级谓词"。

**撤退边界**：仅撤广义断言；v5 §1.2"对象级谓词零命中"与 §2.2 的"运行时取到非授权"一笔带过**必须删除重写成窄断言**。另外，B 路对"原理上不可表达"定理（C2）的构造性反例（`shares_of(doc,grantee,permission)` 是作者自己写出的非重言对象级授权谓词）并不因本窄化而消失——C2 定理的死亡由 D5 另行处理，不在此条。

### D2 · "误报是自己关掉谓词造出来的"

[DA-DECISION: Score 3/5 | Counter | REASON]

答辩用 `unlisted @ aware`（gui 谓词**生效中**）S3 仍 2/2 FP，反驳"误报纯由 blind 开关制造"——我独立核实该格 6/6 全 FP，**反驳成立**，A 路 Q3.2 的"你关掉谓词造 FP"在 unlisted 情形下被证伪。

**反制**：① 答辩承认把 `FP_E3=1.00` 做头条且**隐藏 gui_mode 轴**是呈现缺陷——这是独立伤口，必须改报 2×2 网格；② unlisted+aware 的 FP 仍是**作者代理语义**（GUI 列举不含 unlisted 共享）的产物，不是对 MST-wi 原生行为的独立观测——B 路"两面性来自你自己的代理"的指控未被清除。故：撤"blind 制造说"这一子指控，但持"代理建模说"与"呈现缺陷"两项反制。

### D3 · "E3 探测是循环论证，只是换马甲"

[DA-DECISION: Score 3/5 | Counter | REASON]

答辩**承认** S1–S5 全部共享由框架自 `share()` 创建、无预存共享场景——此为核心实验设计缺口，我接受该承认。其"非循环，是构造式事实"的定性有道理：共享创建独立于被测策略谓词 `can_access`，与 v3"政策谓词无独立来源"不同。

**反制 / 加压**：B 路"三人合一 + 闭环"的更重伤仍成立——探测有效范围 = "共享操作对测试框架可观测"，而真实合法共享（管理员/委派/第三方）恰来自框架外通道。答辩承诺加 S6/S7（预存+不可观测），**但未做**。加压方向：S6/S7 必须实跑，若恢复率跌回 0，则"恢复可判定性"仅限自造闭环。

### D4 · "受控系统是按理论需要长出来的"

[DA-DECISION: Score 3/5 | Counter (Escalate) | REASON]

答辩正面承认被测系统自建、"对象级共享"按理论需要定制，且"写在 Limitations 里≠解决"——诚实。其有限抗辩（对象级共享是主流特性、MST-wi 语料 Jenkins/Joomla 恰无）有道理但**必须用真实系统证**，不能嘴说。

**加压**：crAPI / VAmPI 已排除；候选 Nextcloud / Gitea / GitLab / Joomla 必须实际跑出 E3 场景的触发矩阵，否则"受控系统"始终是致命可行性缺口。这是决定过审的必要条件之一。

### D5 · "全做通也不算贡献"

[DA-DECISION: Score 5/5 | Concede | REASON]

这是答辩最强、且我独立核实的一条。MST-wi 原文定义授权 MR 为"User-A 经 GUI 提供的 URL vs User-B 未经 GUI 提供的同一 URL"+"非 UI 暴露之功能"——**原文本就把判定标准定为 UI 一致性，而非授权状态**。答辩据此把贡献重述为"GUI 提供性不能代理授权状态"，并给出误差方向可由通道可见性预测的机制 + 两侧量化 + 有界修正。该重述**不是稻草人攻击、不是编造、且可证伪**。

B 路"只是缺一个谓词的笔记"在现措辞下成立，但在 D5 重述下被推翻：贡献从"目录缺口笔记"升为"对一篇 TSE'23 工作判定标准的边界刻画"——体量仍薄（短篇/通信级），但**不再是 trivial note**。我撤"strawman / trivial"定性。代价：v5 现有"E3 原理上不可表达 / 重言式"定理式措辞**必须删除**，改述为"MST-wi 的授权 MR 判定基于 GUI 提供性，不基于授权状态；当二者背离时产生可预测的 FP/FN"。

### D6 · 体量与外部有效性

[DA-DECISION: Score 3/5 | Hold | REASON]

答辩承认 2/4 条 MR、2 端点/3 文档、blind/aware 须显式报告——承认到位，但**全部未执行**。A/B 的"2/76 外推无据"指控在补齐前保持成立。Hold：补齐 4 条授权 MR + 真实系统 + 显式 gui_mode 变量，否则外部有效性致命。

### 第 3 节之一 · A 路把"GUI 可达性谓词"等同"授权例外处理"

[DA-DECISION: Score 4/5 | Concede (subsumed by D5) | REASON]

由 D5 已核实的 MST-wi 原文，该 MR 判定的确是 UI 一致性而非授权状态；`userCanRetrieveContent(u,o)` 与 `h(i,o)`（授权政策谓词）**内涵不同、仅在外延上相交**。"存在对象级谓词"与"存在对象级**授权**谓词"的区分是合法边界。本条让步已含于 D5，无新增撤退。

### 第 3 节之二 · A 路用 listed 格 FP=0 论证"对象级判定能消解误报"

[DA-DECISION: Score 4/5 | Counter | REASON]

答辩指出同一 `listed+aware` 格在 S5 有 1 条 FN，要求给出 S3 与 S5 同时正确的配置——我独立核实**不存在这样的配置**（listed+aware：S3 无 FP 但 S5 FN；unlisted+aware：S5 TP 但 S3 FP）。故"listed FP=0 即谓词有效"确为不完备论证，A 路 Q1.3 / Q3.1 据此推翻 C2 的部分被证伪。

**反制**：该"FP↔FN 二选一"结论仍来自**作者自己的代理建模**（GUI 列举决定可见性），不等于"MST-wi 原生判定在 listed 下失效"；且作者仍须修呈现缺陷。故接受经验点，但持"代理归因"反制与"呈现须改"要求。

### 第 3 节之三 · B 路"构造即无效"的举证责任设置不成立

[DA-DECISION: Score 3/5 | Hold | REASON]

答辩称受控实验的正当标准是机制可控性与可反驳性，非"系统必须不是我造的"——此点部分成立：受控机制本身合法。但 B 路的真意是"机制可控性≠对真实系统的外部有效性"，答辩也自认须补真实系统。Hold：接受"受控机制合法"，但"举证责任在我补真实系统"这部分仍须执行，不因本辩而免除。

### 第 4 节反问 R1 · 该 MR 判定的是 UI 一致性还是授权状态？

[DA-DECISION: Score 4/5 | Concede | REASON]

正面回答：**UI 一致性的成分占主导**。MST-wi 原文明文以"GUI 提供的 URL / UI 暴露之功能"为判定基准，未要求角色—资源映射（且自称该映射常不可得）。故"存在对象级谓词"与"存在对象级**授权**谓词"的区别，构成你方 claim 的**合法边界**——你方 D1/D5 据此的重述我接受。

### 第 4 节反问 R2 · 给出 S3 与 S5 同时正确的配置

[DA-DECISION: Score 5/5 | Concede | REASON]

正面回答：**不存在这样的配置**。我遍历全部 (config, gui_mode) 组合独立核实：
- `listed+aware`：S3 无 FP（正确），但 S5 FN（漏报真 BOLA）——错在 S5；
- `unlisted+aware`：S5 TP（正确），但 S3 FP（误报合法共享）——错在 S3；
- 任一 `blind`：S3 恒 FP。

故"对象级判定已经足够"这一判断**无数据支撑**，你的"谓词在 FP 与 FN 之间二选一"成立。A 路据此的"listed FP=0 即消解 E3"被证伪。

### 第 4 节反问 R3 · 举一个确具对象级共享且 MST-wi GUI 爬取能覆盖的开源系统

[DA-DECISION: Score 3/5 | Counter | REASON]

正面回答：**我举不出**一个同时满足"确具对象级共享"且"MST-wi 原生 GUI 爬取能干净覆盖"的系统。Joomla 有 ACL 且 MST-wi 有 OVA，但其对象级*共享*并非强项；Nextcloud/Gitea/GitLab 有对象级共享，但是否落入 MST-wi GUI 爬取覆盖域未经本环境验证（属你方主张，需核实）。

**反制**：你方"MST-wi 的 99.81% 特异性可能部分源于语料恰无对象级共享系统"这一假设**合理、我不予反驳**，但它仍是假设——除非跑原生引擎（C6）在含对象共享系统上复现，否则不能将"两面性"外推到 MST-wi 原生特异性。这正是 C6 必须兑现的理由之一。

---

## 让步统计

| 条目 | 处置 | 是否让步 |
|---|---|---|
| D1 | Concede | 是 |
| D2 | Counter | 否 |
| D3 | Counter | 否 |
| D4 | Counter(Escalate) | 否 |
| D5 | Concede | 是 |
| D6 | Hold | 否 |
| §3.1 | Concede(含于 D5) | 是 |
| §3.2 | Counter | 否 |
| §3.3 | Hold | 否 |
| R1 | Concede | 是 |
| R2 | Concede | 是 |
| R3 | Counter | 否 |

- **Concede 5 条 / 共 12 条 = 让步率 41.7%**，未超过 50%，未触发反谄媚暂停。
- 自查：5 次让步中，D1/D5/§3.1/R1/R2 全部由**独立数据或一手文献**驱动（目录词频、raw_matrix 逐格、MST-wi 原文三句命中、无全对配置），非"看起来合理就撤火"。无连续让步越界（R1→R2 连续，但 R2 按规则升档至 5/5 满足门槛）。
- 仍持 / 加压的 7 条，核心集中在**未执行的修法义务**（预存共享、真实系统、4 条 MR、原生引擎、呈现改网格）。

---

## 更新后的 Critical 清单

| # | Critical | 严重度 | 是否致命 | 作者是否有修法 |
|---|---|---|---|---|
| C1 | 广义"对象级谓词零命中"断言（已被作者自撤，窄化为"授权状态派生谓词为零"） | — | **否（已撤）** | ✅ 已认账并窄化，须重写 v5 §1.2/§2.2 |
| C2 | "E3 原理上不可表达 / 重言式"定理 | 高（现已撤） | **否（作者已重述为 D5）** | ✅ 删除定理措辞，改述"GUI 提供性≠授权状态" |
| C3 | A 路"FN 是误标/弃权"的指控 | — | **否（数据证伪该指控）** | —（作者本就主张两面性，但须注明来自代理语义） |
| C4 | E3 探测仅覆盖框架自建共享，预存共享致盲 | **致命** | **是** | ⚠️ 承诺加 S6/S7，**未做** |
| C5 | 受控系统自建、仅 2 条 MR/2 端点，外推无据 | **致命** | **是** | ⚠️ 承诺补真实系统 + 补齐 4 条，**未做** |
| C6 | MR 为语义移植、未跑原生引擎 | 高 | **是（除非降级为短篇笔记）** | ⚠️ 承诺装 Maven 复核，**未做** |
| C7 | 缺跨厂商独立复核（Codex 网络不可达） | 中高 | 否（已知缺口，可标注） | ⏳ 待网络恢复或换通道 |
| C8 | 呈现缺陷：`FP_E3` 头条隐藏 gui_mode 轴 | 高 | 否（但须改） | ✅ 承诺改报 2×2 网格 |

---

## 对作者反问 R1–R3 的回答

- **R1**：该 MR 判定以 **UI 一致性**成分为主（MST-wi 原文"GUI 提供/UI 暴露功能"为基准，且明示角色—资源映射常不可得）。因此"存在对象级谓词"与"存在对象级**授权**谓词"的区分是你方 claim 的**合法边界**——接受。
- **R2**：**不存在**任一配置使 S3（合法共享）与 S5（只读共享下写越权）同时判对（已独立遍历核实）。故"对象级判定已足够"无依据；你方"FP↔FN 二选一"成立。
- **R3**：**我举不出**同时满足"确具对象级共享"且"MST-wi 原生 GUI 爬取能干净覆盖"的开源系统。你方"MST-wi 99.81% 特异性可能部分源于语料恰缺对象级共享系统"的假设**合理、我不反驳**，但仍是假设，须由 C6 原生引擎在含共享系统上复现方能坐实。

---

## 最终立场

[DA-DECISION: 最终立场 = 有条件可以]

**理由**：D5 借一手文献把贡献合法重述为"GUI 提供性不能代理授权状态"（已核实引用真实），且 D1/D2/R1/R2 的经验反驳由独立数据支撑——论文在**窄化措辞 + 真实系统证据**前提下，可成立为 JISA **短篇 / 通信（note）**，而非方法论文（与 B 路退守线一致）。但当前 v5 全部"修法"停留在承诺，未执行。

**条件分层：**

**A. 不做必被拒（硬门槛，缺任一即"不可以"）：**
1. **C4**：实跑预存共享场景 S6/S7（框架不调 share、DB 直种管理员/第三方共享），报告 Boundary-aware 在该情形恢复率；若跌回 0，明文承认探测仅适用于"框架可观测的共享创建通道"。
2. **C5**：补至少一个**真具对象级共享**的开源系统（Nextcloud/Gitea/GitLab/Joomla 之一）跑出 E3 触发矩阵；并将授权类 MR 由 2 条补至 4 条；gui_mode 作为显式变量报告。
3. **C6**：装 Maven 跑 MST-wi 原生引擎，复核 native 与 port 在 V-E3 上的一致性；若不可行，论文**降级**为"基于 MST-wi 目录语义分析的短篇笔记"，删除所有"实验证明 MST-wi 失效"措辞。
4. **C8**：删除单一 `FP_E3` 头条，改报 2×2 网格（GUI 可见/不可见 × 授权正确/越权），gui_mode 显式声明。

**B. 决定能否过审（软门槛，影响录用层级与可信度）：**
5. **C1/C2**：v5 §1.2/§2.2 改写为窄断言，删除"原理上不可表达/重言式"定理措辞，改述为"MST-wi 授权 MR 判定基于 GUI 提供性而非授权状态"。
6. **C7**：Codex/跨厂商通道恢复后补独立复核；恢复前须在 Limitations 明示"实验完整性判断缺独立性"这一已知缺口。
7. 论文定位明确为 **note / 通信**，不声称方法贡献、不泛化至"MR 范式"、两面性发现须标注"来自本文代理语义，非 MST-wi 原生行为"。
